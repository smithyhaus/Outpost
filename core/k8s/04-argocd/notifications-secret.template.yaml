# =============================================================================
# argocd-notifications-secret — base template.
# -----------------------------------------------------------------------------
# bootstrap.sh CONCATENATES per-plugin fragments into the `stringData:`
# section below. Each notification plugin contributes its `<provider>-webhook-url`
# and (optionally) `<provider>-sign-secret` keys.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: argocd-notifications-secret
  namespace: argocd
type: Opaque
stringData:
