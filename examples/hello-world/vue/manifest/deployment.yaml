apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-vue
  namespace: apps
  labels:
    app: hello-vue
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello-vue
  template:
    metadata:
      labels:
        app: hello-vue
    spec:
      containers:
        - name: app
          image: registry.example.com/hello-vue:latest
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests: { cpu: 50m, memory: 64Mi }
            limits:   { cpu: 500m, memory: 256Mi }
