# Fragment merged into the `stringData:` section of `argocd-notifications-secret`.
# Concatenated by bootstrap.sh — keep the 2-space indent.
  dingtalk-webhook-url: "${DINGTALK_WEBHOOK_URL}"
  dingtalk-sign-secret: "${DINGTALK_SIGN_SECRET}"
