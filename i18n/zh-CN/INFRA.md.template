# 基础设施凭据库（INFRA.md）

> ⚠️ **明文密码**。已 .gitignore，不要提交到任何仓库。
> 备份建议：复制到密码管理器（1Password / Bitwarden）。
>
> 🔁 **复用**：下次新项目要用 PG / Redis / RabbitMQ / Manticore，
>    直接从这里复制连接串，无需重装基础设施。

生成时间：bootstrap 时
根域名：`${ROOT_DOMAIN}`
Registry plugin：`${REGISTRY_PLUGIN}`
Git provider plugin：`${GIT_PROVIDER_PLUGIN}`
Test runner：`${TEST_RUNNER}`(模式:`${TESTKUBE_MODE}`)
Rollout plugin：`${ROLLOUT_PLUGIN}`(默认 `none` —— 可选金丝雀,仅 controller)
通知插件:`${NOTIFICATION_PROVIDERS}`(空 = 未启用)

> v0.3.0：CI 是 GitHub Actions 自托管 runner（§6），CD 是 manifest-sync
> CronJob（§7）。没有 dashboard，没有入站 webhook —— 详见
> [ADR-0003](../../docs/decisions/0003-github-actions-engine-swap.md)。

---

## 0. 速查表

| 服务           | 公网域名                       | 端口 | 用户              | 密码                       |
|----------------|--------------------------------|-----|-------------------|----------------------------|
| PostgreSQL     | `pg.${ROOT_DOMAIN}`            | 5432 | `${POSTGRES_USER}` | `${POSTGRES_PASSWORD}`     |
| Redis          | `redis.${ROOT_DOMAIN}`         | 6379 | （无）            | `${REDIS_PASSWORD}`        |
| RabbitMQ AMQP  | `rabbitmq.${ROOT_DOMAIN}`      | 5672 | `${RABBITMQ_USER}` | `${RABBITMQ_PASSWORD}`     |
| RabbitMQ UI    | `https://mq.${ROOT_DOMAIN}`    | —   | `${RABBITMQ_USER}` | `${RABBITMQ_PASSWORD}`     |
| Manticore HTTP | `https://search.${ROOT_DOMAIN}` | —  | （无）            | — （standalone 无内建鉴权）|
| Registry       | `https://registry.${ROOT_DOMAIN}` | —| (匿名)            | —                          |
| GitHub Actions runner | (宿主机,无公网 URL) | — | — | `${GITHUB_RUNNER_PAT}`(仅用于换取注册令牌) |

没有 ArgoCD / Tekton / Argo Rollouts dashboard 这几行 —— 这些 UI 在
v0.3.0 全部退役。构建状态看 app 仓库的 GitHub Actions 页面（或本机
`journalctl -u actions.runner.* -n 200`）；部署状态用 `outpost status` /
`outpost logs sync`。

---

## 1. Cloudflare

- 根域名：`${ROOT_DOMAIN}`
- DNS 托管：Cloudflare
- Tunnel Token：`${CF_TUNNEL_TOKEN}`
- Tunnel 客户端：Compose 中的 `cloudflared` 容器
- Public Hostname 配置见 `i18n/zh-CN/docs/01-cloudflare-setup.md`

## 2. PostgreSQL（含 pgvector）

- 公网（仅经 cloudflared access）：`pg.${ROOT_DOMAIN}:5432`
- 本机直连（WSL/Linux/macOS）：`localhost:5432`
- K8s pod 内：`postgres.infra-bridges.svc.cluster.local:5432`
- 用户：`${POSTGRES_USER}`
- 密码：`${POSTGRES_PASSWORD}`
- 默认库：`${POSTGRES_DB}`
- 已启用扩展：`vector`、`uuid-ossp`、`pg_trgm`

### 连接串

```
# 应用（K8s 内）
postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres.infra-bridges.svc.cluster.local:5432/${POSTGRES_DB}

# 本机
postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}

# 远端开发机（先 cloudflared access tcp）
postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}
```

DBeaver / DataGrip 客户端配置见 `i18n/zh-CN/docs/04-client-access.md`。

## 3. Redis 7

- 公网：`redis.${ROOT_DOMAIN}:6379`
- 本机：`localhost:6379`
- K8s 内：`redis.infra-bridges.svc.cluster.local:6379`
- 密码：`${REDIS_PASSWORD}`

```
redis://default:${REDIS_PASSWORD}@redis.infra-bridges.svc.cluster.local:6379/0
redis://default:${REDIS_PASSWORD}@localhost:6379/0
```

## 4. RabbitMQ 3.13

- AMQP 公网：`rabbitmq.${ROOT_DOMAIN}:5672`
- AMQP 本机：`localhost:5672`
- AMQP K8s 内：`rabbitmq.infra-bridges.svc.cluster.local:5672`
- 管理 UI：`https://mq.${ROOT_DOMAIN}`
- 用户：`${RABBITMQ_USER}`
- 密码：`${RABBITMQ_PASSWORD}`

```
amqp://${RABBITMQ_USER}:${RABBITMQ_PASSWORD}@rabbitmq.infra-bridges.svc.cluster.local:5672/
amqp://${RABBITMQ_USER}:${RABBITMQ_PASSWORD}@localhost:5672/
```

## 5. Manticore Search 7.4.6

- 公网 HTTP：`https://search.${ROOT_DOMAIN}`（JSON API，不是 UI）
- K8s 内 HTTP：`http://manticore.infra-bridges.svc.cluster.local:9308`
- K8s 内 SQL：`mysql -hmanticore.infra-bridges.svc.cluster.local -P9306`
- 鉴权：无（standalone 模式无内建鉴权）
- Replication 钩子：见 `core/compose/manticore/README.md` 一键开启

```bash
# 健康探测（HTTP）
curl https://search.${ROOT_DOMAIN}/

# 本地 SQL 接入
mysql -h127.0.0.1 -P9306 -e 'SHOW STATUS'
```

## 6. CI —— GitHub Actions 自托管 runner

- 安装目录：`$HOME/actions-runner`（systemd 服务 `actions.runner.*`）
- Runner URL：`${GITHUB_RUNNER_URL}`（推荐 org 级 —— 一个 runner 服务 org
  下所有私有仓）
- 注册用 PAT：`${GITHUB_RUNNER_PAT}` —— 仅用于经 GitHub API 换取短生命周期
  的 runner *注册* token；不是 runner 长期凭据，不会被打印到日志。
  **留空 = 跳过 runner 安装/校验并打印醒目 WARN**（这是 CI/e2e 场景下的
  合法状态，不是配置错误）。
- Labels：`${GITHUB_RUNNER_LABELS}`（默认 `outpost`）
- Runner 名称：`${GITHUB_RUNNER_NAME}`（留空 → `outpost-<hostname>`）
- 已注册的 app 仓库：`${OUTPOST_REPOS}` —— 逗号分隔的 `<clone-url>[#branch]`
  列表。这是 `verify.sh` 对账的基准；用 `outpost onboard <url>` 注册新仓库。
- 纯出站长轮询（50s）访问 `api.github.com` —— **没有入站端口，没有
  webhook secret，防火墙不需要开任何口子。**

查状态：

```bash
systemctl status 'actions.runner.*'
journalctl -u 'actions.runner.*' -n 200
```

Pipeline 行为（`templates/github/outpost-build.yml`，push 到部署分支触发）：

```
1. push（gitee 为主；双推或 gitee→github 镜像保持 github 同步）
2. github.com 派发 workflow → runner 接单（长轮询）
3. checkout       浅克隆到被 push 的 commit
4. build-image.sh  buildctl → 127.0.0.1:30750（buildkitd NodePort）
                    push → 集群内 registry，tag = 7 位 commit SHA
5. run-tests.sh    Gate A：outpost.test.yaml / Dockerfile.test（可选）
6. update-manifest.sh 克隆 gitee manifest 仓，改 tag，push
                       （6 次抖动重试）
7. 任一步失败 → notify-fanout.sh build-failed
```

## 7. CD —— manifest-sync CronJob

- 命名空间：`outpost-ci`
- 间隔：`${MANIFEST_SYNC_INTERVAL}` 分钟（`concurrencyPolicy: Forbid` ——
  单写者，绝不与自己重叠）
- Manifest 仓库：`${MANIFEST_REPO_URL}`（branch `${MANIFEST_REPO_BRANCH}`）
- 对账滞后阈值：`${OUTPOST_STALENESS_THRESHOLD}` 秒 —— live HEAD 与已部署
  tag 不一致最多能拖多久，超过 `verify.sh` 判 FAIL 并点名仓库
- Registry tag 保留数（= 回滚可达深度）：`${OUTPOST_REGISTRY_KEEP_TAGS}`

查状态：

```bash
outpost status                    # sync-heartbeat 摘要
outpost logs sync                 # 最近一次 manifest-sync Job 日志
kubectl get configmap sync-heartbeat -n outpost-ci -o yaml
```

回滚：

```bash
outpost rollback <app> [sha]      # 纯国内路径 —— github 挂了也能用
```

**破玻璃逃生门：** `kubectl rollout undo` 依然可直接生效，但必须配合
manifest revert（`outpost rollback`）—— manifest 仓库是强制执行的真相源，
不配合 revert 的 `rollout undo` 会被下一次 sync 顶回去。

## 8. Docker Registry

- 当前 plugin：`${REGISTRY_PLUGIN}`
- 公网：`https://registry.${ROOT_DOMAIN}`
- 集群内（containerd 已配置走明文）：`registry.${ROOT_DOMAIN}/<image>:<tag>`

## 9. Git 提供商

- 用户：`${GIT_USER}`
- Personal Access Token：`${GIT_TOKEN}`
- Manifest 仓库：`${MANIFEST_REPO_URL}` (branch `${MANIFEST_REPO_BRANCH}`)
- Token 用途：manifest-sync HTTPS 拉、`update-manifest.sh` push
  （workflow 侧，跑在 runner 上）

## 10. 主机 / 系统

- outpost 目录：`${SK_INFRA_DIR:-~/outpost}`
- kubeconfig：`~/.kube/config`
- k3s 数据：`/var/lib/rancher/k3s/`（Linux/WSL2）
- sealed-secrets 公钥备份：`secrets-backup/sealed-secrets-pub.pem`

### 常用命令

```bash
./status.sh         # 全栈状态快照
./verify.sh         # 详细健康检查（AI 用 --json）
./upgrade.sh        # 拉最新镜像 + 重启 Compose
```

---

## 11. 测试网关、自动回滚、多通道告警

完整设计见 `i18n/zh-CN/docs/proposals/cicd-test-gate.md`。
快速入门走 `i18n/zh-CN/docs/00-quickstart.md` 的 Phase J 章节。

### 当前启用的 plugin

- 测试运行器:`${TEST_RUNNER}`(模式:`${TESTKUBE_MODE}`)
- 回滚 plugin:`${ROLLOUT_PLUGIN}`
- 通知通道:`${NOTIFICATION_PROVIDERS}`

### 端点

- 没有 dashboard —— v0.3.0 里 `ROLLOUT_PLUGIN` 仅装 controller，没有
  Argo Rollouts dashboard 这一层。金丝雀状态看
  `kubectl argo rollouts get rollout <name> -n apps`（kubectl 插件）
  或 `kubectl get rollout -n apps`。
- Testkube agent:命名空间 `testkube`(`kubectl get pods -n testkube`)
- Argo Rollouts controller（仅当 `ROLLOUT_PLUGIN=argo-rollouts`）:
  命名空间 `argo-rollouts`

### 告警 fan-out

三个事件会 fan-out 到所有启用的通道，经由 `scripts/notify-fanout.sh`：
`build-failed`（GitHub Actions workflow）、`deploy-succeeded`/
`deploy-failed`（manifest-sync CronJob）、`verify-failed`
（`outpost-verify.timer` systemd 定时器）。改 `.env` 里
`NOTIFICATION_PROVIDERS`(逗号分隔) 后重跑 `bash bootstrap.sh` 即可调整。

| 通道              | webhook URL 变量          | 加签 secret(可选)         |
|-------------------|---------------------------|----------------------------|
| dingtalk          | `DINGTALK_WEBHOOK_URL`    | `DINGTALK_SIGN_SECRET`     |
| feishu            | `FEISHU_WEBHOOK_URL`      | `FEISHU_SIGN_SECRET`       |
| wecom             | `WECOM_WEBHOOK_URL`       | (无 — 直接保护 URL 本身)   |
| webhook-generic   | `GENERIC_WEBHOOK_URL`     | `GENERIC_WEBHOOK_BEARER`   |

### 测试约定(应用仓库根目录)

```yaml
# outpost.test.yaml
version: 1
runner:
  command: ["sh", "-c", "go test ./..."]   # 或 pytest / npm test / mvn test / dotnet test
gates:
  pre-deploy:        { timeout: 5m }
  post-deploy-smoke: { enabled: true, image: curlimages/curl:8.10.0,
                       command: ["sh","-c","curl -fsS http://my-app.apps.svc.cluster.local/healthz"],
                       timeout: 30s }
```

仓库根没有 `outpost.test.yaml` 也没有 `Dockerfile.test` → Gate A 干净 skip,
pipeline 照常跑。

### 自动回滚阈值(已锁定)

默认 `AnalysisTemplate`:`failureLimit: 2`、`consecutiveErrorLimit: 3`、
`successCondition: result == "Passed"`(Job provider)或 `result == 200`
(Web HTTP probe)。

## 12. 安全加固清单（建议但不强制）

- [ ] 给 `mq.${ROOT_DOMAIN}` / `search.${ROOT_DOMAIN}` 加 Cloudflare
      Access(邮箱 OTP / IdP)—— v0.3.0 已没有 BasicAuth dashboard 可叠加，
      这是目前唯一的边缘鉴权面
- [ ] 给 `registry.${ROOT_DOMAIN}` 加 Basic Auth 或 Cloudflare Access(默认匿名)
- [ ] `${GITHUB_RUNNER_PAT}` 只给最小权限（仅换取注册 token），runner
      注册完成后尽快撤销/轮换 —— 日常运行不需要它
- [ ] GitHub Actions runner group 保持仅私有仓可见（GitHub 默认）——
      永远不要让自托管 runner 跑公开仓库的 PR
- [ ] sealed-secrets 公钥备份至密码管理器

## 13. 复用清单（新项目接入流程）

详见 `i18n/zh-CN/docs/05-onboard-project.md`。简略：

1. 在 Gitee/GitHub/GitLab 建仓库，根目录加 `Dockerfile`
2. `outpost onboard <repo-url>` —— 注册进 `OUTPOST_REPOS`（`verify.sh`
   对账的基准），打印 CI workflow + 双推设置步骤
3. 把 `templates/github/outpost-build.yml` 拷进应用仓库的
   `.github/workflows/`（onboard 命令会主动提议帮你拷）
4. 配置双推（`git remote set-url --add --push origin <url>` 对 gitee 和
   github 各配一次）或 gitee→github 单向镜像 —— **全程不需要在任何地方
   注册 webhook**
5. manifest 仓库加 `apps/<name>/{deployment,service,ingress,
   kustomization}.yaml`（`outpost onboard --manifests-dir <path>
   --lang <lang>` 会帮你 scaffold）
6. `git push`（双推配好后一次 push 两边都到），看 `outpost logs sync` ——
   `manifest-sync` 会在 `${MANIFEST_SYNC_INTERVAL}` 分钟内收敛

应用代码里要用的连接串直接从本文件 §0 速查表复制。
